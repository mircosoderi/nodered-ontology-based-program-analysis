{
  "name": "jsonld-flatten-check",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "description": "CLI to verify whether a JSON-LD document is already flattened according to jsonld.flatten() output.",
  "main": "index.js",
  "scripts": {
    "check": "node index.js"
  },
  "dependencies": {
    "jsonld": "^8.3.3"
  }
}

